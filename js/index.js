 /* open model  */
 const openModel = () => {
    const model = document.querySelector('.model');
    model.style.display = 'block';
};

const closeModel = () => {
    const model = document.querySelector('.model');
    model.style.display = 'none';
};

const openAddModel = () => {
    const model = document.querySelector('.add-model');
    model.style.display = 'block';
};

const closeAddModel = () => {
    const model = document.querySelector('.add-model');
    model.style.display = 'none';
};

const openDeleteModel = () => {
    const model = document.querySelector('.delete-model');
    model.style.display = 'block';
};

const closeDeleteModel = () => {
    const model = document.querySelector('.delete-model');
    model.style.display = 'none';
};

const openEditModel = () => {
    const model = document.querySelector('.edit-model');
    model.style.display = 'block';
};

const closeEditModel = () => {
    const model = document.querySelector('.edit-model');
    model.style.display = 'none';
};


const openUserModel = () => {
    const model = document.querySelector('.user-model');
    model.style.display = 'block';
};

const closeUserModel = () => {
    const model = document.querySelector('.user-model');
    model.style.display = 'none';
};

const openNotificationModel = () => {
    const model = document.querySelector('.notification-model');
    model.style.display = 'block';
};

const closeNotificationModel = () => {
    const model = document.querySelector('.notification-model');
    model.style.display = 'none';
};